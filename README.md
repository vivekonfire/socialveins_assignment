# Infinte Loader with a fallback button

### Build with:

-   React.js - Front-end Framework
-   Zustland - State Management Library

### To Run

-   Clone the repo
-   Run `npm install`
-   Run `npm run start`
